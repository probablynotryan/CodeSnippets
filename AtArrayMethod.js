let arr = ['amanda', 'bob', 'cindy', 'dylan', 'satan'];

arr.at(0); // = 'amanda' (same as arr[0])
arr.at(1); // = 'bob' (same as arr[1])
arr.at(-1); // = 'satan' (same as arr[arr.length - 1])
arr.at(-5); // = 'amanda'